name: Setup Julia
description: Setup Julia runtime
inputs:
  julia-version:
    description: Julia version
    required: false
    default: '1.10'
runs:
  using: composite
  steps:
    - uses: julia-actions/setup-julia@v2
      with:
        version: ${{ inputs.julia-version }}

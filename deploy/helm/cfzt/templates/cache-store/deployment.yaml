apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cfzt.fullname" . }}-cache-store
  namespace: {{ include "cfzt.namespace" . }}
  labels:
    {{- include "cfzt.labels" . | nindent 4 }}
    app.kubernetes.io/component: caching
spec:
  replicas: {{ .Values.cacheStore.replicas | default 1 }}
  selector:
    matchLabels:
      {{- include "cfzt.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: caching
  template:
    metadata:
      labels:
        {{- include "cfzt.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: caching
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: cache-store
          image: "{{ .Values.cacheStore.image.repository }}:{{ .Values.cacheStore.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.cacheStore.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - name: http
              containerPort: {{ .Values.cacheStore.service.port | default 8007 }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "cfzt.fullname" . }}-cache-store-config
          {{- with .Values.cacheStore.env }}
          env:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.cacheStore.resources | nindent 12 }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL

apiVersion: v1
kind: Service
metadata:
  name: analytics
  namespace: cfzt
  labels:
    app: analytics
    component: analytics
spec:
  type: ClusterIP
  selector:
    app: analytics
  ports:
    - name: http
      port: 8009
      targetPort: 8009
      protocol: TCP
